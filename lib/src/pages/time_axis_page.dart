import 'package:flutter/material.dart';

class TimeAxisPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("TimeAxisPage"),
    );
  }

}